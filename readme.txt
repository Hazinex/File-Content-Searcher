Fundamentally, this software searches through a directory for every file,
Then it looks to see if those files contain a substring inputted by a user.

Version 1.0 has support for the following file types:
docx
xlsx
pptx
pdf
